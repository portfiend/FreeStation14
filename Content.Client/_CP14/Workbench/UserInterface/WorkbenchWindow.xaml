<DefaultWindow xmlns="https://spacestation14.io"
               xmlns:graphics="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
               xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
               xmlns:workbench="clr-namespace:Content.Client._CP14.Workbench.UserInterface"
               Title="{Loc 'workbench-ui-window-title'}"
               SetSize="700 500"
               MinSize="700 300">
    <BoxContainer Orientation="Vertical" SeparationOverride="6">
        <!-- Topbar -->
        <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
            <!-- Search Bar -->
            <LineEdit Name="SearchBar" PlaceHolder="Search" HorizontalExpand="True" />
            <OptionButton Name="OptionCategories" Access="Public" MinSize="130 0" />
        </BoxContainer>

        <!-- Main -->
        <BoxContainer HorizontalExpand="True"
            VerticalExpand="True"
            Orientation="Horizontal"
            SeparationOverride="5">
            <!-- Product list (left side UI) -->
            <BoxContainer SetWidth="350"
                Orientation="Vertical">
                <!-- Crafts container -->
                <ScrollContainer
                    HorizontalExpand="True"
                    VerticalExpand="True"
                    HScrollEnabled="False">
                    <BoxContainer Name="CraftsContainer" Orientation="Vertical" HorizontalExpand="True" VerticalAlignment="Top"/>
                </ScrollContainer>
            </BoxContainer>

            <!-- Craft view (right side UI) -->
            <BoxContainer MinWidth="350"
                Orientation="Vertical"
                HorizontalExpand="True"
                VerticalExpand="True">
                <ScrollContainer VerticalExpand="True" HScrollEnabled="False">
                    <PanelContainer
                        StyleIdentifier="WorkbenchRequirementList"
                        HorizontalExpand="True"
                        VerticalExpand="True">
                        <!-- Content -->
                        <BoxContainer HorizontalExpand="True" VerticalExpand="True" Orientation="Vertical">
                            <!-- Item info -->
                            <workbench:WorkbenchRecipeResultInfo Name="ResultInfo"/>

                            <controls:HLine Color="#404040" Thickness="2" Margin="0 5" />

                            <!-- Required title -->
                            <Label Margin="5 0 0 0" Text="{Loc 'workbench-ui-requirement-list-header'}" />

                            <!-- Craft requirements content -->
                            <!-- Added by code -->
                            <BoxContainer
                                Margin="5 0 0 0"
                                Name="ItemRequirements"
                                Orientation="Vertical" VerticalExpand="True"
                                HorizontalExpand="True" />

                            <controls:HLine Color="#404040" Thickness="5" Margin="0 5" />

                            <!-- Craft button -->
                            <Button Name="CraftButton" Disabled="True" Text="{Loc 'workbench-ui-craft-button'}" />
                        </BoxContainer>
                    </PanelContainer>
                </ScrollContainer>
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</DefaultWindow>
